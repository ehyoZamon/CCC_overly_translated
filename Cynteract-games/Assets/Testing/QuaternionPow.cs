using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[ExecuteInEditMode]
public class QuaternionPow : MonoBehaviour
{
    public Transform normalQube, powedQube;
    [Range(-10, 10)]
    public float pow;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
